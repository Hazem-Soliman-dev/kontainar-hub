import { createMetadata } from "../../../../lib/seo/metadata";

const metadata = createMetadata({
  title: "Supplier Analytics Insights",
  description:
    "Analyze revenue trends, product performance, and regional demand in the TajirJomla Hub supplier analytics workspace.",
  path: "/supplier/analytics",
  noIndex: true,
});

export default metadata;
