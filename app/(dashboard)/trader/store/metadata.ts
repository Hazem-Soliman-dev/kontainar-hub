import { createMetadata } from "../../../../lib/seo/metadata";

const metadata = createMetadata({
  title: "Trader Store Management",
  description:
    "Create and manage sub-stores for each selling channel, region, or assortment in TajirJomla Hub.",
  path: "/trader/store",
  noIndex: true,
});

export default metadata;
