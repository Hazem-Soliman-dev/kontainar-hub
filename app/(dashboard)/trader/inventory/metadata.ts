import { createMetadata } from "../../../../lib/seo/metadata";

const metadata = createMetadata({
  title: "Trader Inventory Intelligence",
  description:
    "Track inventory health across suppliers and stores with real-time status updates in TajirJomla Hub.",
  path: "/trader/inventory",
  noIndex: true,
});

export default metadata;
