import { createMetadata } from "../../../../lib/seo/metadata";

const metadata = createMetadata({
  title: "Trader Overview Dashboard",
  description:
    "Monitor supplier relationships, real-time inventory, and omnichannel stores inside the Kontainar Hub trader dashboard.",
  path: "/trader/dashboard",
  noIndex: true,
});

export default metadata;
